include 'backend:auth', 'backend:client-profile', 'backend:chat', 'backend:event', 'backend:organizer-channel', 'backend:organizer-profile', 'backend:rating-system'
include 'backend:rating-system'
findProject(':backend:rating-system')?.name = 'rating-system'

