server:
  port: 8711

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/diploma
    username: artem
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect