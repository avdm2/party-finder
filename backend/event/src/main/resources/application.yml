server:
  port: 8723

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/diploma
    username: artem
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect